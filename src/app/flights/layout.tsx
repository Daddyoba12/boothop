// TravelPayouts Drive is loaded site-wide in the root layout — no extra script needed here.
export default function FlightsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
